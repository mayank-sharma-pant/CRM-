# Phase 6.8 implementation plan

1. `LoginResult` parser + tests (MFA challenge is not a session).
2. Auth repository/provider, interceptor Bearer, verify + enroll screens, router.
3. Profile link to 2FA settings (enable/disable when session exists).
4. `flutter_app/store/` Play/App listing + `flutter create` instructions.
5. Mark 6.8 DONE (code) with upload residual.
