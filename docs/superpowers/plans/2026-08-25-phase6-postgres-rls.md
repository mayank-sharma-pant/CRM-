# Phase 6.10 plan

`app/tenancy.py` + `after_begin` SET LOCAL; `get_current_user` / platform admin bind; public-path middleware bypass; `enable_rls` in `create_missing_tables.py`. Tests: `tests/tenancy/test_rls.py`.
