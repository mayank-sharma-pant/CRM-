'use client';

/**
 * MANAGER CLIENTS PAGE
 * 
 * Reuses the Sales Clients Page strictly.
 * Data scope (Team vs Personal) is handled by the API layer based on the route context.
 */

import Clients from '../../sales/clients/page.jsx';

export default Clients;
