'use client';

/**
 * MANAGER DASHBOARD
 * 
 * Reuses the Sales Execution Dashboard strictly.
 * Data scope (Team vs Personal) is handled by the API layer based on the route context.
 */

import Dashboard from '../../sales/dashboard/page.jsx';

export default Dashboard;
